<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sa.ksu.swe444">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/growth_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AwardsActivity"></activity>
        <activity android:name=".Upload_post" />
        <activity
            android:name=".ClassMainActivity"
            android:parentActivityName=".MainActivity"
            android:label="@string/title_activity_class_main"
            android:theme="@style/AppTheme.AppBarOverlay" />
        <activity android:name=".ParentMainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LoginSignUpActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ChatActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>